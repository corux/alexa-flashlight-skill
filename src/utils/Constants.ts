export class Constants {
    /** Default time for the Echo light ring in seconds. */
    public static readonly DEFAULT_TIME = 300;
    /** Maximum time for the Echo light ring in seconds. */
    public static readonly MAX_TIME = 3000;
    /** Timeout of the GameEngine Input Handler in milliseconds. */
    public static readonly INPUT_HANDLER_TIMEOUT = 30000;
}
